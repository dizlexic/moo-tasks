NUXT_SESSION_PASSWORD=replace-with-a-secure-random-string-of-at-least-32-characters

BASE_URL=http://localhost:3000
# Application Port
PORT=3000
HOST=localhost

# Database Configuration (for docker-compose)
DB_ROOT_PASSWORD=password
DB_USER=mootasks
DB_PASSWORD=password
DB_NAME=mootasks
DB_PORT=3307
DB_HOST=localhost

# Application Secrets
# Need at least 32 characters

# SMTP Configuration
SMTP_HOST=smtp.ionos.com
SMTP_PORT=587
SMTP_USER=info@mootasks.dev
SMTP_PASSWORD=replace-with-actual-password
SMTP_FROM=info@mootasks.dev
